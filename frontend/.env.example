# Copy to .env.
#
# DEV: leave VITE_API_URL EMPTY. The Vite dev server proxies /api/* to the
# Flask backend (see vite.config.js), so the browser only needs port 5173.
VITE_API_URL=
#
# PROD (Vercel): set to your absolute Render backend URL, e.g.
# VITE_API_URL=https://debugmymind-api.onrender.com
